## Example
# whether import `proj.android.streaming` project
buildWithAgoraProjAndroidStreaming=true
# exclude asan, for local debug
asanExclude = true
# exclude abi, for speed, default false
# this config shared by `proj.android.streaming` and `ut`
abiExcludeArm=true
abiExcludeArm64=false
abiExcludeX86=true
# when import `proj.android.streaming`, `proj.android.streaming` will read this prop
buildStreamingWithNative=true
# when import `proj.android.streaming`, `proj.android.streaming` will read this prop
gnoutDir=/Users/xhn/media_sdk_script/rtmp_streaming_kit/proj.android.streaming/.aclient
# when import `proj.android.streaming`, `proj.android.streaming` will read this prop，`ut ` deps libagora_streaming_kit_instrumentationtests_so，set to true
includeTest=true